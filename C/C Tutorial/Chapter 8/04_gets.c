#include<stdio.h>
int main(){
    char st[30];
    gets(st);//The entered string is stored in st
    // printf("%s", st);
    puts(st);//Prints the string and puts the cursor in the next line( or we can say break line)
    
    return 0;

}